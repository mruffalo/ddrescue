Changes in version 1.2:

A new option `--complete-only' that instructs ddrescue not to read new
data beyond logfile limits, has been added.

A new argument parser that replaces `getopt_long' has been added.

The interval at which the logfile is saved to disk now depends on
logfile size.
