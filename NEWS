Changes in version 1.4:

A new option "--direct" that instructs ddrescue to use direct disc
access to read the input file, bypassing the kernel cache, has been
added.

A bug that displayed an incorrect initial error size has been fixed.
